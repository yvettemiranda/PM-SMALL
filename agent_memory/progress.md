# Progress

## 当前任务

- 修复用户2026-08-06提出的最终TEST界面和筛选问题：新增市场生命周期进度上限筛选（1%至100%）、排除总持续时长不足1天的市场、把逐市场多选做成明确模块且默认全选、为按钮异步操作增加转圈及耗时/完成时间，并清晰区分自动挂出的买入委托与成交后持仓。完成本地验证和审查后推送GitHub `main`、同步Linux服务器并复核安全状态；全过程保持`PAPER + LIVE_DISABLED`，不接钱包，不把短时部署验收算作正式长期验证。

## 当前状态

- 用户2026-08-05 18:18截图确认手机5G在登录挑战前返回`ERR_CONNECTION_CLOSED`，并说明Wi-Fi同样失败，因此不是用户名或密码错误。
- 快速反馈循环连续3次直连固定公网IPv4的HTTPS均在TLS阶段超时；同机HTTP裸IPv4建立TCP后收到空响应。独立美国公网抓取仍得到HTTPS 401，说明服务器和认证代理在线，但裸IPv4链路对当前用户网络不可用。
- 同一台服务器改用IP派生主机名后，本机HTTP立即得到404、HTTPS在约1秒完成TLS并得到401；当前证书尚只覆盖IPv4，因此该次HTTPS诊断使用了忽略证书校验。该单变量结果把根因收敛到裸IPv4访问路径，下一步必须为主机名签发正式证书并改写跳转入口后再用严格证书校验复验。
- 新增`scripts/check-public-access.sh`作为可重复公网验收入口：它严格检查主机名证书、HTTP跳转、未认证401、认证后主页/API 200、`PAPER + LIVE_DISABLED + PAUSED`以及SQLite周期验证；当前对候选主机名稳定因证书名称不匹配退出，已具备修复前红灯信号，禁止以`--insecure`绕过。
- Chrome、浏览器扩展和本机消息宿主诊断均正常，但服务器页面的控制连接多次在列举、创建和恢复标签页时超时；按浏览器安全规则，下一步需用户允许打开新的Chrome窗口后再继续服务器修改，不能绕过为其他桌面自动化方式。
- Linux服务器以GitHub `main`提交`a628b3ef8254123333b91a59a14265ab57a6baca`完成干净镜像构建；首次运行阶段重复`npm ci`导致`better-sqlite3`本地编译失败的问题已修复并在服务器重建通过。容器以非root用户运行，宿主`data/`持久化，受控重启后恢复`healthy`。
- Docker应用只监听`127.0.0.1:3000`；Nginx监听公网80/443，HTTP 308跳转至固定IPv4 HTTPS，未认证返回401，正确凭据访问主页、CSS、JavaScript和轻量状态均返回200。反向代理清除上游认证头、拒绝浏览器明确跨站请求并设置基础安全响应头。
- Let's Encrypt固定IPv4短周期证书签发成功；Certbot 5.7.0定时器与部署后Nginx校验/重载钩子已配置，续期干运行通过。登录明文仅保存在服务器仓库外的`0600`文件，Nginx哈希文件为`0640 root:www-data`，两者均未进入Git。
- 腾讯云原TCP 3000单IP临时规则已精确删除，80/443继续开放；独立公网抓取经HTTP跳转后得到HTTPS 401，证明跨网络入口可达且认证边界生效。自动化浏览器自身无法通过其代理直连裸IPv4，因此手机/电脑实际浏览器登录仍需用户做最终人工确认。
- HTTPS及应用容器受控重启后，模式仍为`PAPER`、`liveExecutionEnabled=false`、策略`PAUSED`，资金100U、盈亏0，周期验证`passed=true`、`sqliteIntegrity=ok`，三个PAPER后台服务运行。该结果只算部署冒烟，正式长期验证未启动。
- 已完整读取 `HANDOFF.md`、`docs/DECISIONS.md`、`docs/PLAN.md`。
- 2026-08-06本轮实现已贯通：SQLite迁移8持久化`maxMarketProgressPercent`；总时长不足完整1天固定排除；进度上限1%至100%、默认20%，候选扫描、自动/手动新买及活动买单复核同一设置；保存配置会立即请求自动调度、刷新订阅并重扫。旧的隐藏90%截止默认已改为最终100%边界，部署时必须同步服务器`.env`。
- 最终UI已新增进度滑杆、明确的逐市场多选与默认全选、待成交买入委托说明、按钮转圈/经过秒数和数据/扫描完成时间；保存按钮持续等待本次扫描结束。未成交买入委托明确不算持仓，只有点击`开始TEST`后才自动挂单。
- 真实构建产物已用24个候选和1张未成交买单完成1024px、390px、320px验证：默认20项、二元/三元和市场选择全选；进度25%改5%后候选24项缩为5项；扫描期间保存按钮持续转圈计时，完成后显示更新时间；三种宽度无横向溢出且浏览器控制台无警告或错误。临时服务与SQLite已可恢复地移到`/Users/d4clt/.Trash/pm-small-ui-smoke.cmEXL9`。
- 全量测试初次发现两个旧库测试夹具未跟随新增起止字段和迁移8；已修正为接受新增元数据，并只重放本测试目标的迁移6/7。Node.js 24.14.0类型检查、23个测试文件/129项全量测试、构建、源文件与构建产物前端语法检查及`git diff --check`随后全部通过。并行验证时构建清理`dist/`与产物语法检查发生一次竞态导致路径暂时不存在，构建完成后串行复跑通过；仍需提交、推送和服务器部署。
- 本轮相对`de71b7b`完成Standards与Spec双轴本地审查：首审发现缓存候选可绕过实时进度、待成交数量受最近100单截断、配置面板提前关闭使保存反馈不可见，现已分别用请求时进度复核、全量活动买单计数和扫描完成后再收起面板修复并补回归；复审无剩余P0/P1/P2/P3，未扩大PAPER、LIVE、钱包或长期验证边界。
- 最终TEST UI已替换旧宽表格：桌面和手机均为单列竖向布局，只显示资金盈亏、当前持仓和扫描市场；配置按需展开，持仓结果方向醒目，扫描默认20项并每次再显示20项。
- 新增SQLite持久化的二元/三元、最高买入价、离散总时长和Token结果方向选择；运行时扫描动态读取设置，取消选择会撤销对应未成交PAPER买单，已有持仓和卖单继续保留。
- 状态和持仓接口现提供总资金、总/已实现/未实现盈亏、市场链接、结果方向、加权平均买入价和全生命周期进度；估值优先使用完整盘口最佳买价并保守回退。
- 最终UI首轮双轴审查提出的价格范围、筛选即时生效、完整持仓统计、任意Token选择、旧持仓元数据、非法旧配置、验证冻结原子性、轮询覆盖草稿和可访问性问题均已修复并补回归测试；最高买入价固定为整数`1¢`至`3¢`，设置在自动及手动新买入口同步复核。复审另发现的主按钮12px白字对比度问题已用现有深青色修复，对比度从约3.88:1提高到约5.41:1并通过最终确认。
- 本轮固定三个公开测试边界：HTTP开始/暂停/新一轮控制、偏好保存响应、SQLite账本周期重置。筛选保存必须在同一事务中撤销因结果数、价格或总时长不再合格的活动PAPER买单；新一轮只解锁数量与成本均为0、尚未正式结算的已完成Token，并在账本验证通过后进入`RUNNING`。
- 上述三项已实现：顶部开始/暂停可实际控制TEST；偏好保存原子撤销不再合格的活动买单；显式新一轮保留未完成持仓、卖单、资金和历史盈亏，且正式结算市场不解锁。普通开始只续跑当前账本。
- Node.js 24.14.0类型检查、23个测试文件/121项全量测试、构建、前端语法检查和`git diff --check`已通过。真实浏览器发现并修复空POST错误请求头后，开始、暂停、配置保存及新一轮均复验通过；桌面、390px和320px无横向溢出，控制台无错误。
- 本轮相对`cba40b3`的Standards与Spec双轴审查均无P0/P1/P2/P3；确认原子撤买、周期锁、正式结算永久锁、资金与历史保留、手机UI及PAPER/LIVE安全边界均符合已确认规则，没有范围膨胀。
- 真实本地服务的桌面、390px与320px浏览器验收已通过；配置保存、LIVE锁定、进度正序/倒序、`20 + 20`展开、单项选择、全选和清空均正常，控制台无警告或错误。320px初检发现`html/body min-width`导致滚动条宽度溢出，移除该约束后复验无横向溢出。
- 已完整检查`awesome-design-md`固定提交`8147538b4226ae41e2487a9179e3bcc1f68e8554`的README、目录及金融/交易/紧凑移动候选文件；最终前三为Vercel、Binance、Coinbase，并明确不复制品牌配色、营销留白、横向滚动表格或实盘紧迫感。
- 三版交互对比已通过静态结构检查，以及桌面、360px手机和320px窄屏自动验证；配置展开、市场类型多选、价格同步、总时长节点、TEST/LIVE锁定、进度正倒序、全选/清空、单项排除和`20 + 20`展开均正常，未发现横向溢出或脚本错误。
- GitHub `origin/main`是唯一交付基线；最终TEST UI功能提交`e1186cb`和审查修正提交`616ef20`已推送，本次交接记录也须在提交后复核最终远端SHA。
- 官方 `listEvents` 分页迭代器现会遍历总时长安全窗口内的全部开放事件页面；候选 Token 不再按固定数量截断，事件每页默认100项，订单簿仍以50个 Token 分批读取。
- 事件及市场继续校验 active、closed、archived、acceptingOrders、enableOrderBook 和 Condition ID；总时长由 `MAX_MARKET_DURATION_DAYS` 筛选，默认30天。
- 已实现默认每60秒运行的只读PAPER验证，覆盖SQLite、资金守恒、订单成交范围、挂单占用、持仓卖单覆盖和结算残留，并通过状态及即时API暴露结果。
- 验证失败会保留现场、记录审计并暂停策略；除卖单继续减仓外冻结自动账本变更，账本未恢复通过前不能重新进入 `RUNNING`。
- 已增加`EVENTS/ORDER_BOOKS/COMPLETE/FAILED`阶段、分页/批次/规模/动态耗时诊断；只读JSONL运行器会采样安全状态、账本验证、服务状态、告警和停止原因。
- 真实30天窗口单轮扫描耗时308.239秒，遍历12,631个事件、读取10,972个合格Token订单簿并生成2,452个候选；新版5秒留证冒烟观察到11页/1,100个事件且无关键错误。
- 双轴审查提出的关键问题已修复：累计周期失败、零有效样本、单接口关键异常被另一接口超时掩盖；runner改用轻量状态，截止时间前不再多发一次请求。
- 单变量公网探针确认：836 Token省略`initial_dump`时19秒内没有初始`book`，显式设置后约3.7秒收到838条`book`；新适配层约2.72秒达到836/836完整盘口。
- 候选扫描已贯通`AbortSignal`，WebSocket关闭与重连等待有界，服务器信号关停具备5秒总截止线；扫描中`SIGINT`冒烟以0退出。
- 新链路完整30天扫描耗时252.208秒，遍历12,639个事件、读取10,770个订单簿并生成2,405个候选；2,405/2,405初始盘口全部就绪并稳定超过60秒，周期验证6次失败0次，大连接`SIGINT`约319毫秒以0退出。
- 2,400 Token断线注入首次约5.5秒就绪；主动关闭后第一次重连遇到1006异常，自动再次重试并在约10.9秒恢复2,400/2,400，最终`lastError=null`。
- 新一轮默认30天扫描用247.335秒遍历135页/13,475个事件和7,744个订单簿，得到2,099个候选；首次完整盘口用时3.524秒。连续3次断线均先降为0个就绪盘口，再在5.868、5.472和5.627秒恢复2,099/2,099，最终累计连接/完整盘口/异常断线/恢复为4/4/3/3且`lastError=null`。
- 重复断线JSONL证据SHA-256为`ced4dd81727722d36c7ac2953cc1b06cbec4a3a3c74ca44c4dc1acd25f1ba553`；本机忽略探针脚本SHA-256为`98bab3194767c3e5a3cb400d57eb9342e43a25d7e4b99b9d3d117313da8074b4`。自动测试新增连续3次断线后每轮恢复完整盘口的固定回归覆盖。
- 10分钟隔离PAPER留证正常完成：20样本、传输错误0、关键错误0；扫描后连续10样本告警0，最大候选/订阅2,386，自动买单100张，周期验证1至10次均通过。
- 离线SQLite完整性`ok`，100张开放买单占用与99,990,510微单位保留资金相等，账本差额0；同库重启恢复100单和100/100相关Token盘口后正常关停。证据JSONL与数据库保存在忽略目录`data/validation/`。
- 证据SHA-256：JSONL `6ee5518c64d966baaa7d782bfd911a87ed54ee1c2dcb8085257d32dcea9b9ba7`；数据库 `96c2706f2e5abbc398764de9caf914097a5432737d476535dee5bfd4b889ecf2`。
- 行情状态现累计连接、完整盘口、异常断线、恢复和实际PAPER买卖成交；JSONL汇总保留首末值和窗口最大值，避免采样间隔或进程重启掩盖活动。
- 15分钟隔离PAPER运行正常完成：28样本、关键错误0；受控同库重启造成2次连续传输失败后恢复，未触发3次停止阈值。两轮完整扫描分别为13,433事件/7,848订单簿/2,067候选和13,440事件/7,856订单簿/2,080候选；最终2,082/2,082盘口完整。
- 重启后100张活动买单和100/100对应盘口在0.911秒恢复；SQLite完整性`ok`，开放买单名义占用99,990,000微单位与保留资金一致，成交/持仓/结算仍为0。证据哈希：JSONL `90c705128d03992dcca3f082082966644ab0d49ea4d5daa3437ba77678938b8e`；数据库 `fe92991bd641dff1f8ef3749deb6648e5e5690284fd8fa000609c4d3de46f2d5`。
- 从上述健康数据库创建隔离副本后，将策略保留资金由99,990,000微单位注入为0；周期验证自动把策略置为`PAUSED`，即时验证返回503，重新启动返回409。失败留证24样本后以6项关键错误退出，`maxFailedValidationCount=1`。
- 恢复正确占用后即时验证和重新启动通过；同库冷启动恢复100张活动买单、100/100完整盘口，15个恢复样本传输错误0、关键错误0、失败验证0。最终SQLite完整性`ok`，资金占用相等，成交/持仓/结算均为0。
- 故障演练证据哈希：检测JSONL `af742006061edf467b76e8160f88107e68e06a768ed641a4952530c0631fe003`；恢复JSONL `99b78fa35cf51cd81ea0d8e8b0979589659113986fde370309e5436c9a8531e7`；最终数据库 `350dbb2fa322c617480b81f5b5004a07d3ecf12c5684385f1218be16d4aedf97`。
- Node.js 24.14.0类型检查、23个测试文件/114项全量测试、构建、前端语法检查和`git diff --check`已通过；桌面、390px和320px复验无控制台错误或横向溢出，未保存草稿在10.5秒轮询后不丢失，保存后旧候选即时过滤。最终 Standards/Spec 双轴复审均无P0/P1/P2/P3，钱包与LIVE边界保持不变。
- 隔离账本故障文档增量的 Standards/Spec 双轴审查均无P0/P1/P2/P3；证据数值、SHA-256、未完成边界及Git忽略状态均复核一致。
- 三轮行情恢复增量的 Standards/Spec 双轴复审均无P0/P1/P2/P3；可控测试源同时保留错误式断线覆盖和三轮正常结束覆盖，Promise拒绝与关闭清理边界复核通过。
- 候选服务TDD已复现等待`refresh()`结束仍返回`scanning=true`；最小调整Promise完成顺序后，服务与API测试确认“成功→暂时失败并保留候选→再次成功恢复”，订阅者和手动刷新都收到完成态快照。补充订阅断言时曾误放到相邻测试作用域，已立即纠正并重新通过定向测试。
- 候选扫描暂时失败恢复增量的 Standards/Spec 双轴审查均无P0/P1/P2/P3；审查确认失败保留、诊断暴露、成功恢复、订阅/API完成态及长期验收边界均符合规格。
- 进程资源留证TDD已复现状态接口、样本和汇总缺少运行时数据；最小实现后，定向测试确认完整/轻量状态均返回运行时长与内存，JSONL样本保留原值，汇总保留窗口最大值。
- 实际构建产物以独立SQLite、停止策略运行2.2秒端到端冒烟：5样本、传输错误0、关键错误0；最大RSS 451,395,584字节、最大堆已用135,581,432字节，扫描未完成产生5个预期告警。忽略证据SHA-256为`b6c7cc15f55e63586bdb766b95c5d7a0eb91bf58c1bce3ef4e8325cd3eee297f`，只验证资源留证接线，不作为Linux容量基线。
- 清理本轮临时冒烟目录时，直接递归删除被环境安全策略拒绝；已改为可恢复地移动到`/Users/d4clt/.Trash/pm-small-runtime-smoke.feEfnY`，正式JSONL证据仍保留在Git忽略目录。
- 资源留证首轮Spec审查无发现；Standards审查提出1项P2判断性Data Clumps/Duplicated Code，五项峰值曾在类型、初始化和更新三处展开。现已收拢为`RuntimeResourceMaxima`与统一更新函数，对外JSONL字段不变。
- 修复后Node.js 24.14.0类型检查、22个测试文件/96项测试、构建和`git diff --check`全部通过；Standards/Spec复审均无P0/P1/P2/P3。
- TDD已覆盖事件第二页限流后只重试相同游标、订单簿`503`后只重试相同批次、匹配引擎`425`最多3次、永久性`400`不重试，以及退避期间取消；扫描诊断合并事件与订单簿请求恢复计数，长期样本对恢复行为告警并汇总窗口最大值。Node.js 24.14.0类型检查、22个测试文件/102项测试、构建和`git diff --check`均已通过。
- 全市场请求恢复增量的Standards/Spec双轴审查均无P0/P1/P2/P3；固定SDK分页接口、Promise与取消清理、计数聚合、扁平长期汇总、市场筛选及PAPER/LIVE边界均复核通过。
- 代码提交`73141a1`和文档提交`8492fab`已推送至GitHub `main`；同步后本地、`origin/main`和GitHub远端一致，领先/落后为`0/0`，工作树干净。
- 长时间运行预检已通过：代码基线`14dddab833a9b0cee9ab4739a86f2ac6d84f3ede`与GitHub一致，Node.js 24.14.0类型检查、22个测试文件/102项测试、构建及`git diff --check`通过；磁盘可用约390GiB，端口3210未占用。候选证据使用Git忽略路径`data/validation/paper-long-20260805T030215Z.{db,jsonl}`。
- 2026-08-05T03:03:14Z启动预检已确认`executionMode=PAPER`、`liveExecutionEnabled=false`、策略`RUNNING`、四项后台服务运行、SQLite完整性`ok`；用户明确长期验证由其制定计划后，监测在35.277秒立即中断并写入`INTERRUPTED`汇总，服务随后安全退出且端口3210无监听进程。
- 本次预检仅产生1个样本，首次扫描停在30页/3,000事件，关键错误、请求重试、限流及临时错误均为0；这不是完整扫描或长期验证证据。JSONL SHA-256为`bb4dc83fedcf3c58d0b760dd56ef5c63f884be540b9b74d47505b5d27cfd336c`，数据库SHA-256为`779db7239ec392cb81672e6d7693cd41fa1e56678d17f95b2c35125fdf6a0c87`，两者均在Git忽略目录且不得纳入正式验收。
- 离线核对初次查询误用了数据库不存在的`position_cost_micros`列，未发生写入；读取`PRAGMA table_info`后按实际schema重新检查，最终`PRAGMA integrity_check=ok`，订单、成交、持仓和结算记录均为0。

## 下一步

- 用户明确允许打开新的Chrome窗口后，恢复腾讯云终端控制；为选定主机名签发受信任证书、把HTTP跳转和Nginx主机配置改为该入口，并保持应用3000只监听本机。
- 使用不忽略证书校验的本机反馈循环、独立公网401探针及用户手机5G/Wi-Fi实际登录三层复验；原裸IPv4链接不再作为交付入口。
- 修复完成后整理完整登录、查看、配置、开始/暂停TEST及异常处理步骤，先向用户确认收件人为微信“文件传输助手”和确切文字，再通过剪贴板粘贴发送。
- 人工访问确认后等待用户制定并确认长期验证计划；确认前不启动真实长时间PAPER运行或故障注入。
- 候选扫描原位有界重试和最终失败保留均已有固定回归覆盖；在正式长期窗口中继续保留真实或受控限流、暂时接口失败与恢复证据。
- 短时三轮WebSocket断线恢复已通过；在正式长期运行窗口中继续观测自然或受控断线，确认同样的全量恢复率和耗时边界。
- 长期窗口内必须实际观察或故障注入纸面买入、自动卖单和首次卖出后的撤买链路；10分钟冒烟未产生可确认成交。
- v0.5.0长期验收完成前继续保持钱包/LIVE禁用。
- 本轮代码、证据、审查和同步记录均按完成后立即推送并复核远端SHA的流程处理；服务器实际部署在UI完成后、正式长期验证前执行。
