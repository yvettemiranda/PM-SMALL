# Progress

## 当前任务

- 在 GitHub `main` 基线 `31b9dae3e1a93a34ad70d47e32221efe25a6f8c9` 上完成 v0.5.0 的 TEST 可执行闭环、最终手机/桌面 UI、GitHub 同步和 Linux 服务器同提交部署。
- 服务器短时冒烟完成后停止；正式长期验证由用户另行制定计划。

## 已完成

- 本地功能提交 `fe5ff57`：全量监控、实时 Best Ask FAK 买入、目标 Best Bid FAK 卖出、动态费用、自动下一轮、TEST 重置、紧凑单列 UI 和公开 TEST API。
- 总时长规则为每个具体市场 `1天 <= 总时长 <= 滑杆上限`，默认 30 天；生命周期进度只展示/排序。
- 双轴审查发现并已修复：事件时间误代替具体市场时间；买入后同一盘口已达目标却等待下一 tick；旧卖单迁移后最小下单量为 0；逐 Token 选择残留；手动新一轮接口；公开长期验证仍使用旧 PAPER 模式和接口。
- 扫描改为遍历全部开放事件分页，不使用事件日期窗口，避免事件与子市场时间不一致造成漏选。
- 交易编排接受任意已启用执行适配器，公共执行结果不再暴露 `PaperOrder` 类型；当前 LIVE 适配器仍无条件禁用。
- 审查修正后的全量测试 25 个文件、157 项通过；类型检查、构建、前端语法、Shell 语法和 `git diff --check` 均通过。
- 本地构建产物已完成桌面、390px 和 320px 浏览器验收：无横向溢出，配置草稿、开始/暂停、LIVE 锁和 500ms 刷新正常，控制台无错误。
- 审查修正的目标回归、类型检查及 TEST 长期监测接口回归已通过。

## 正在进行

- 提交本地审查修正并推送 GitHub `main`，随后部署服务器同一提交。

## 后续步骤

1. 提交并推送 GitHub `main`，核对远端 SHA。
2. 只读盘点服务器；停止应用后备份 SQLite 与配置并校验。
3. 部署同一 SHA，执行 HTTPS/认证、`TEST + LIVE_DISABLED + PAUSED`、迁移、SQLite、页面及同库重启短时冒烟。
4. 更新交接记录并再次推送/同步。
5. 在正式长期验证前停止，交给用户制定计划。
