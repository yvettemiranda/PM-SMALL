# Progress

## 当前任务

- 在GitHub `main`基线`31b9dae3e1a93a34ad70d47e32221efe25a6f8c9`上完成v0.5.0：把旧等待成交的PAPER maker模型改成可真实映射到未来LIVE的TEST即时成交模型，收尾手机/电脑共用UI，验证后推送GitHub并部署同一提交到Linux服务器。
- 本轮只允许`TEST + LIVE_DISABLED`，不接钱包、签名或真实订单；服务器短时冒烟完成后停止，正式长期验证由用户另行制定计划。

## 当前状态

- 已完整读取`HANDOFF.md`、`docs/DECISIONS.md`、`docs/PLAN.md`及项目记忆文件；当前仍在未提交工作树中开发，尚未推送或部署。
- 市场扫描遍历所选安全时间窗口内全部分页，不设置候选数量上限；只保留开放、未关闭/归档、允许下单、启用订单簿且有Condition ID的二元/三元市场，再读取对应订单簿。
- 市场总时长规则已按用户最终确认实现为`1天 <= endsAt-openedAt <= 所选上限`，滑杆节点为`1、7、14、30、60、90、120、180、360、365`，默认30天；剩余时间不参与该时长筛选。生命周期进度只用于显示和正序/倒序，不再作为隐藏买入截止条件。
- 全部结构合格Token进入实时监控；只有当前Best Ask大于0、低于等于用户价格上限且每轮金额满足交易所最小下单量时才进入候选并触发买入。无历史成交量硬过滤。
- TEST买入按实时订单簿Best Ask执行FAK：逐档成交，允许部分成交，未成交余量立即取消，不保留等待买单，也不复用已消耗盘口；每个Token每轮累计现金上限默认1U。
- 每笔买入成交立即建立`max(买价+1¢, 买价×1.5)`并按tick向上取整的目标卖单；Best Bid达到目标后按FAK减仓，部分成交继续等待。卖单按更低目标价优先使用有限买盘，首次卖出后本轮禁止继续买入。
- 一个Token完整卖出后，只要市场仍开放且未正式结算，会自动开始下一轮，不再要求人工点击；正式结算后永久停止该Condition的新轮次。市场关闭且官方状态确认后，TEST自动结算/赎回并计入净盈亏。
- 买卖均读取市场动态费用；买入费用减少所得份额，卖出费用减少到账金额。持仓按当前可执行Best Bid扣除预计卖出费用估值；明确无买盘时可兑现价值按0处理，避免虚高TEST资金与盈亏。
- TEST与未来LIVE共用候选排序、买入/卖出意图和策略计算，执行通过适配器隔离；当前`TestExecutor`可用，`LiveExecutorDisabled`全部失败关闭。
- UI已改为手机优先单列：顶部TEST/LIVE锁、按需配置、总资金/总盈亏/已实现/未实现、当前持仓、扫描市场；无最近活动、逐Token勾选或程序诊断卡片。配置包含类别、二元/三元、最高买价、总时长、总模拟资金和每轮金额；候选默认20项并可展开，价格以两位小数美分显示，页面约500ms刷新。
- TEST默认100U、每轮1U、初始`PAUSED`；重置要求暂停及两次确认，并清空资金、订单、成交、持仓、结算、配置和审计后恢复默认。LIVE仍锁定。
- 新增回归已覆盖：总时长1天边界、99%生命周期仍可买、实时价格降至上限立即买、同盘口配置变化重新判断、盘口深度不复用、FAK部分买卖、费用、目标价优先、完整卖出后自动下一轮、无买盘估值为0、非法空市场类型和TEST重置。
- 当前针对性回归25项通过，另一次核心范围回归66项通过；TypeScript类型检查和前端JavaScript语法检查通过。全量测试、构建、真实浏览器验收、双轴代码审查、文档更新、GitHub推送和服务器部署尚未执行。

## 下一步

- 清理剩余兼容字段对外展示、同步版本号和扫描状态文案，核对结算/验证/迁移边界。
- 运行全量测试、类型检查、构建、`git diff --check`，再用真实本地服务完成桌面、390px和320px交互/无溢出/控制台验收。
- 按`code-review`流程完成Standards与Spec双轴审查并修复发现；更新`HANDOFF.md`、`docs/DECISIONS.md`、`docs/PLAN.md`、`README.md`及项目记忆。
- 提交并推送GitHub `main`，确认本地、`origin/main`和GitHub远端SHA一致。
- 先备份服务器SQLite和部署配置并校验哈希，再部署同一Git提交；执行HTTPS、认证、健康、UI、迁移、SQLite及`TEST + LIVE_DISABLED + PAUSED`短时冒烟。
- 到正式长期验证边界立即停止，交由用户制定验证计划；不得接入钱包或启用LIVE。
