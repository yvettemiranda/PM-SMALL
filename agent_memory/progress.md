# Progress

## 当前任务

- 用户已授权把此前确认的手机 UI、筛选与扫描稳定性问题全部优化完成，并推送 GitHub、备份后部署服务器。
- 交付必须保持 `TEST + LIVE_DISABLED + PAUSED`；本任务不启动正式 TEST、不接钱包、不开发或启用 LIVE，也不把部署冒烟算作长期验证。

## 已完成

- 统一资格函数已用于扫描清单、自动买入、手动买入和数据库最终保护：固定最低买价 1¢、有效 Best Bid、可配置 Bid/Ask 比例（默认 50%）、生命周期进度硬门槛（默认 `<=20%`）、1 至所选最大天数、官方类别、结果数量、完整订单簿、最小下单量和体育开赛时间。
- 扫描保留全部结构合格 Token 供 WebSocket 监控，只向页面和新买入暴露当前完整满足所有资格的市场；扫描失败保留上一轮，完成后等待 15 秒且不重叠。
- TEST 买卖按实时订单簿 FAK 执行，每笔买入填充独立生成目标；盘口深度消费按单侧稳定状态版本写入 Schema 12，重复帧、等价快照及重启恢复均不重复使用。
- 新 Token/新周期只有可用现金足以覆盖用户配置的整笔轮次金额才开启；已有部分周期可在新盘口出现后按剩余上限补买，资金释放后同盘口候选会重新尝试。
- TEST 初始资金只可在 `PAUSED` 且无交易历史时修改；重置清空全部 TEST 状态与盘口消费。Schema 12 升级原子执行并强制 `PAUSED`。
- UI 已把类别收敛为 Polymarket 首页当前 14 个栏目并动态同步、默认全选；总时长改为 1–365 天内自由上下限。资金摘要为总资金/持仓实时价值/持仓数单行，TEST/LIVE 缩小，排序改为单按钮，持仓默认前 20 项。
- 本地构建产物已在 462px Codex 侧边栏验收：栏目同步与全选、7–30 天输入、三项资金单行、单排序按钮均可见，无横向溢出且控制台无错误。
- 双轴审查发现并修复相同/重放盘口重复成交、资金释放后不重试、体育开赛规则分叉、升级未暂停、迁移非原子、伪 `OTHER` 类别、资金不足提前清空周期及 tick 缺失未失败关闭。
- 本轮最终全量测试 26 个文件、192 项通过；类型检查、前端脚本语法检查、生产构建和 `git diff --check` 全部通过。
- 隔离预览首轮真实扫描已完成：18,195 个事件、34,876 个结构合格 Token、34,874 份订单簿、698 批请求，耗时 879,892ms（约 14 分 40 秒），无错误、无限流，最终候选数为 0。
- 扫描诊断确认两个独立原因：首轮完成前候选服务不会发布中间结果，页面会持续误显示 0；首轮完成后仍为 0，则说明当前全部筛选条件的组合在该行情快照下确实没有交集。现有诊断没有逐规则淘汰计数，尚不能把结果归因到某一个单独条件。
- 已部署网页版在用户放宽条件后实际发布过 23 个候选；同一轮新扫描仍处于 `EVENTS`、`lastScanAt` 未变化时，候选数已从 23 降为 19，排除“只有完整重扫替换结果”这一单一解释。
- 同期行情流订阅约 34,860 个 Token，连接累计 10 次、意外断开 9 次、完整快照 0 次且无接口报错。代码确认每次行情流重启都会把旧订阅 Token 的候选报价设为 `bookReady=false`，Dashboard 又实时剔除未就绪盘口，因此候选会在断线/重连期间暂时消失，随后随行情快照到达而逐步重新出现。
- 聚焦测试 `MarketStreamService > reconnects after the market stream ends` 通过，确认当前实现确实会在断线时把候选 `bookReady` 置为 `false`，重连收到快照后才恢复；本轮只诊断，尚未新增回归测试或修改运行代码。
- 已新增并通过增量行情订阅回归：候选集合变化时在同一 WebSocket 上分片 `subscribe/unsubscribe`，保留 Token 不再断线或清空；1,201 Token 的订阅分片不丢 Token。
- 扫描现先按结果数、Polymarket 首页类别、自由时长区间和生命周期进度预筛，再拉取匹配 Token 的盘口；价格过高或暂时无盘口的同条件 Token 仍进入监控池，不设本地数量上限。
- 订单簿请求改为默认 4 路有界并发、每批 50 Token，并保留原顺序、重试、限流与取消语义；新增 151 Token/2 路并发回归证明无丢失和无越界并发。
- 扫描诊断新增静态合格数、盘口目标数及逐规则淘汰计数；Dashboard 区分可交易候选和保留展示候选。断线时保留最后有效报价并标记 `RECONNECTING/NOT_READY`，自动交易仍由实时完整盘口硬门禁。
- 手机 UI 已接入事件/盘口扫描进度、监控数、可交易数和主要淘汰原因；重连候选显示上次价格与明确状态，不再从列表瞬间消失。
- 当前新增功能聚焦回归 7 个文件、57 项通过，类型检查通过。
- 大规模实扫进一步定位出两个平方复杂度热路径：候选报价按数组线性查找、每条行情遍历全部订阅判断完整快照；另发现 TEST 运行时单 Token 变化会重排全监控池。现改为 Token 索引、待快照集合和变更 Token 定向评估，三组聚焦回归共 24 项通过。
- 本轮首次直接调用 `pnpm` 被工作区依赖策略包装器在测试前拦截，未改依赖或锁文件；已切换为 bundled Node 直接运行仓库现有 Vitest，红绿回归均获得有效结果。
- 热路径修复后全量自动验证为 26 个文件、203 项测试通过；类型检查、生产构建、前端语法和 `git diff --check` 均通过。
- 隔离真实扫描在 `PAUSED`、7–30 天、Bid/Ask ≥1%、进度 ≤20%、类别全选下遍历 18,197 个事件，静态保留并获取 2,544 个 Token 盘口，51 批请求零重试、零限流；首轮约 5 分 4 秒完成并找到 1 个可交易市场，单 WebSocket 的 2,544/2,544 完整快照耗时 3.95 秒，零断线。
- 第二轮完整扫描和第三轮开始期间候选始终保留，连接累计仍为 1；完成后 RSS 约 314MB。第三轮扫描与侧边栏 500ms 轮询并行时，SIGINT 约 0.96 秒退出且退出码 0。
- 462px 与 320px 浏览器核验无横向溢出；14 个栏目全部选中、7–30 天正确、资金三项单行、扫描进度和单排序按钮正确，未保存草稿未被轮询覆盖，控制台无警告或错误。
- GitHub 功能代码已同步到 `40a09e36a9cb1bd27f3b39a11bc96b71083ba4ee`；服务器两份备份均通过 SHA-256 和 SQLite 检查，Schema 13 部署、容器重启、公网认证、端口边界和严格访问脚本均通过。
- 服务器旧 FAK 不变量已无损修复，验证恢复通过；现有 1000U 历史资金保留，当前时长设为 30 天，重置默认值校正为 100U/100U/1U/30 天。

## 正在进行

- 正在基于本地 UI 提交 `639dfe63c922bd249f9f4f31f14fe82d00baedc0` 完成热路径修复后的全量回归、本地真实多轮扫描、资源/优雅停止和手机 UI 验证；随后进行双轴审查、GitHub 推送及服务器同 SHA 部署。

## 后续步骤

1. 完成全量测试、构建、本地真实多轮扫描与 462px/320px 手机 UI 验证，确认扫描耗时、内存、候选跨轮稳定和 WebSocket 连接计数。
2. 完成 Standards / Spec 双轴审查并修复全部发现，提交并推送 GitHub `main`。
3. 停止容器后备份服务器数据与配置并校验，再部署 GitHub 同一 SHA；复核 Schema、SQLite、HTTPS、认证、端口和 `TEST + LIVE_DISABLED + PAUSED`。
4. 部署完成即停止在正式长期 TEST 验证前，等待用户共同制定计划。
