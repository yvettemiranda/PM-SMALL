# 项目交接记录

更新时间：2026-08-05（UTC）

## 当前结论

项目已完成`v0.4.0`纸面结算闭环，并已进入`v0.5.0`长期验证开发。开放市场扫描先按用户配置的市场总时长上限（默认30天）构造不漏候选的开始/结束时间窗口，再遍历窗口内官方接口全部分页并执行精确资格筛选。当前已有周期PAPER账本验证、失败暂停门槛和只读留证工具；候选扫描暂时失败时会保留上一批有效候选并在下一次成功后恢复，隔离账本不一致恢复及全市场规模三轮连续断线恢复演练也已通过。正式长时间运行、剩余故障矩阵和持续容量验收尚未完成，仍不得接入钱包或用于实盘。

本文件所在的`main`分支最新提交为当前交接基线，具体版本以Git提交记录为准。

## 已完成

- 建立Node.js 24、TypeScript、Fastify、SQLite项目结构；
- 接入Polymarket公开市场数据；
- 开放市场扫描使用与总时长规则等价的时间窗口，并通过官方分页迭代器遍历窗口内全部事件，不再只读取第一页或截断固定数量的候选Token；
- 实现事件过滤、候选Token提取和市场扫描；
- 实现1至3美分Maker买价、单笔1U和固定卖价计算；
- 实现保守纸面队列成交模型；
- 接入官方公开市场WebSocket及自动重连；
- 公开Market订阅显式请求`initial_dump: true`，避免大订阅只收到增量行情却缺少初始盘口；
- 断线后暂停成交，收到新盘口快照后重新建立保守队列；
- 实现实时成交去重、纸面自动部分成交和盘口增量更新；
- 每批买入成交后自动建立固定价格纸面卖单；
- 首次卖出成交后撤销剩余买单并永久停止该轮新买；
- 仅在Token完整盘口快照就绪后自动建立合格纸面买单；
- 体育比赛开始后撤销未成交买单并禁止重新挂买；
- 市场进度达到配置上限（默认90%）后撤销买单未成交部分；
- 用户暂停或停止时撤销买单未成交部分，保留持仓和卖单；
- 重启时核对资金守恒、挂单占用和持仓卖单覆盖关系，异常自动暂停；
- 默认每60秒只读核对SQLite、资金守恒、挂单占用、持仓卖单覆盖和结算残留；
- 周期验证失败时保留不一致现场并暂停策略，除卖单继续减仓外冻结自动账本变更，验证恢复前禁止重新运行；
- 已在隔离SQLite副本中注入保留资金与活动买单占用不一致，验证周期服务自动暂停、即时校验返回503、重新启动返回409；修复账本并冷启动后恢复100张活动买单和100/100盘口；
- 提供周期验证状态、轻量`GET /api/status?compact=true`和`GET /api/paper/validation`即时检查接口；
- 实时记录每次全市场扫描的阶段、事件分页、事件、合格Token、订单簿批次、候选数量和耗时；
- 候选扫描暂时失败时保留上一批成功结果并暴露错误；后续成功扫描替换候选并清除错误，等待手动刷新完成时返回`scanning=false`；
- 轻量状态及长期JSONL样本记录进程运行时长、RSS、堆总量、堆已用量和外部内存，汇总保留观察窗口最大值；
- 提供只读JSONL长期PAPER留证运行器及停止条件；
- 长期状态累计记录行情连接、完整盘口、异常断线、恢复耗时和实际PAPER买卖成交；行情、成交、自动买入/撤买、结算和验证计数统一汇总首末值与窗口最大值，服务器重启不会掩盖重启前活动；
- 已在2,099个全市场候选Token上连续注入3次公开行情断线，每次均先清除就绪盘口，再恢复2,099/2,099完整快照；
- 候选扫描支持取消，WebSocket关闭与重连等待有界，进程信号关停具备5秒总截止线；
- 建立纸面订单、成交、持仓、策略状态和审计表；
- 读取Polymarket正式结算状态和二元结果，保守等待未最终确认的市场；
- 支持官方`50/50`结果的按比例纸面结算；
- 结算时撤销活动纸面订单，模拟获胜Token赎回、失败仓位归零和资金回收；
- 以Condition ID保证结算幂等，记录检查、回收、收益和冲突审计；
- 提供基础网页界面和状态、候选市场、纸面订单、持仓、结算接口；
- 实盘执行器固定禁用；
- 建立Docker、自动测试和跨账号交接基础。

## 尚未完成

- 正式长时间纸面运行和剩余故障注入矩阵；
- 全市场扫描在真实 API 长时间运行下的速率限制、响应规模和 WebSocket 订阅容量验证；
- 约2,000至2,400 Token完整规模下的长时间稳定性、长期窗口内断线恢复和重复扫描复验；
- 钱包、签名、实盘订单、撤单、对账和链上赎回。

## 下一步

进入`v0.5.0`长期纸面验证阶段：

1. 按`docs/PAPER_SOAK.md`使用周期验证和扫描诊断进行长时间PAPER运行；
2. 重复结算结果已有固定幂等测试，候选扫描暂时失败与恢复已有固定服务/API测试；继续在长期窗口留证并补齐其余故障场景，账本不一致及短时三轮断线演练的证据原件不得修改；
3. 核对全市场扫描性能、接口速率、WebSocket订阅容量和恢复后的状态；
4. 在长期运行中重复注入断线并统计重连次数、恢复耗时和盘口重新就绪率；
5. 完成长期验证前继续保持钱包和LIVE禁用。

长期验证完成前仍不得接入钱包、提交真实订单或执行链上写操作。

## 最近验证

当前`main`工作树本次验证结果：

- 使用Node.js 24.14.0运行等价类型检查命令：通过；
- 使用Vitest全量测试：22个测试文件、96项测试通过；
- 使用构建脚本：通过；
- `git diff --check`：通过；
- 基线记录的依赖审计为0项漏洞；本轮未重新运行审计；
- 真实PAPER短时冒烟完成一轮30天默认窗口扫描：308.239秒内遍历12,631个事件、读取10,972个合格Token订单簿并生成2,452个候选；周期和即时账本验证均通过，执行模式保持PAPER且LIVE关闭。
- 旧订阅链路在2,452个Token连接后完整盘口数为0；进一步对836个Token做单变量探针，省略`initial_dump`时19秒内`book=0`，显式设置`initial_dump: true`后约3.7秒收到838条`book`事件，确认根因不是SDK解析或100 Token限制。
- 新公开行情适配层先在约2.72秒达到836/836个Token完整盘口，句柄约336毫秒关闭；随后完整30天扫描在252.208秒内遍历12,639个事件、读取10,770个订单簿并生成2,405个候选，2,405/2,405初始盘口全部就绪且稳定观察超过60秒，周期验证6次失败0次。
- 2,405 Token连接下`SIGINT`约319毫秒完成，退出码0；扫描进行中关停也以0退出。完整规模就绪与有界关停已通过本轮短时验收，长期稳定性和重复断线仍待验证。
- 2,400 Token真实断线注入中，首次全量盘口约5.5秒就绪；主动关闭后第一次重连遇到服务端1006异常，服务保持盘口未就绪并自动再次重试，约10.9秒恢复到2,400/2,400，最终`lastError=null`。单次恢复通过，重复断线和长期统计仍待执行。
- 独立数据库10分钟PAPER留证运行正常完成：20个有效样本、传输错误0、关键错误0；扫描完成后连续10个样本告警0，最大候选/订阅2,386，周期验证从1增至10且失败0，自动建立100张PAPER买单。
- 离线SQLite完整性为`ok`，100张开放买单名义占用99,990,510微单位，与策略保留资金完全一致且未解释差额为0；同库重启恢复100张活动买单、100/100相关Token盘口及周期验证均通过。该10分钟结果不替代正式长期运行，也尚未覆盖真实纸面成交和卖出链路。
- 本机忽略目录证据为`data/validation/paper-smoke-20260804.jsonl`（SHA-256 `6ee5518c...ba7`）和同名`.db`（SHA-256 `96c2706f...cf2`）；它们未纳入Git，跨机器接手时需单独复制。
- 新版累计诊断的15分钟隔离运行正常完成：28个有效样本、关键错误0；受控同库重启造成2次连续本机传输失败后自动恢复，未达到3次停止阈值。首轮扫描为13,433事件/7,848订单簿/2,067候选，盘口2,067/2,067且就绪耗时2.609秒；第二轮为13,440事件/7,856订单簿/2,080候选，活动订单合并后盘口2,082/2,082且就绪耗时2.454秒。
- 重启后100张活动买单和100/100对应盘口在0.911秒恢复，离线SQLite完整性`ok`；100张开放买单名义占用99,990,000微单位，与策略保留资金一致，成交/持仓/结算均为0。本轮没有观察到真实低价Token成交，受控运行时测试已覆盖买入成交、自动卖单、首次卖出撤买和账本通过，真实长期成交仍待观察。
- 15分钟本机忽略目录证据为`data/validation/paper-stage-20260805.jsonl`（SHA-256 `90c70512...8b8e`）和同名`.db`（SHA-256 `fe92991b...f2d5`）；原始运行使用补窗口最大值前的汇总版本，因此最终行的`lastPlacedBuyCount=0`反映重启后的进程计数，逐样本记录和离线数据库均确认窗口内最大自动买单数为100。新版CLI集成测试直接注入`placedBuyCount: 100→0`并确认`maxPlacedBuyCount=100`。
- 新版诊断的5秒只读冒烟记录到`EVENTS`阶段11页/1,100个事件、0个关键错误；这只验证诊断和留证链路，不替代正式长时间验收。
- 隔离账本故障演练先在健康副本上确认100张开放买单名义占用与保留资金均为99,990,000微单位，再把保留资金注入为0。周期服务将策略自动切为`PAUSED`，即时校验返回503，手动`POST /api/paper/start`返回409；失败留证在24个样本后以6项关键错误退出，`maxFailedValidationCount=1`。
- 修复占用后即时校验通过且策略可重新启动；同库冷启动恢复100张活动买单、100/100完整盘口，15个恢复样本以`COMPLETED`结束，传输错误0、关键错误0、失败校验0。最终SQLite完整性`ok`，策略为`RUNNING`，活动买单占用与保留资金仍精确相等，成交/持仓/结算均为0。
- 本机忽略目录证据为`data/validation/paper-ledger-fault-detect-20260805.jsonl`（SHA-256 `af742006...e003`）、`paper-ledger-fault-recovered-20260805.jsonl`（SHA-256 `99b78fa3...1e7`）和`paper-ledger-fault-20260805.db`（SHA-256 `350dbb2f...f97`）。演练只证明该类不一致能失败关闭并经核对后恢复，不替代正式长期运行或其他故障场景。
- 三轮全市场行情断线探针在247.335秒内遍历135页、13,475个事件和7,744个订单簿，得到2,099个候选Token；首次完整盘口耗时3.524秒。三次断线后就绪数均先归零，再分别在5.868、5.472和5.627秒恢复2,099/2,099，最终累计4次连接、4次完整快照、3次异常断线、3次恢复且`lastError=null`。
- 重复断线JSONL证据为`data/validation/market-stream-repeat-20260805.jsonl`（SHA-256 `ced4dd81...a553`），本机只读探针脚本SHA-256为`98bab319...74b4`；两者均在Git忽略目录。新增自动测试固定覆盖连续3次断线后完整盘口恢复，但短时结果仍不替代正式长期稳定性。
- 本轮 Standards/Spec 首审发现的3个P2已用统一计数窗口、完整活动计数和`100→0`重启测试修复；复审未发现新的P0/P1/P2/P3。
- 隔离账本故障增量再次通过 Standards/Spec 双轴审查；证据数字、哈希、阶段边界和操作手册均无P0/P1/P2/P3。
- 三轮行情恢复增量通过 Standards/Spec 双轴复审；错误式断线、三轮正常断线、逐轮未就绪与全量恢复均有固定覆盖，无P0/P1/P2/P3。
- 候选扫描故障回归固定覆盖“成功→暂时失败→成功”：失败时保留上一批候选并暴露错误，恢复后替换候选并清除错误；服务订阅者和`GET /api/candidates?refresh=true`均在刷新完成后观察到`scanning=false`。
- 候选扫描暂时失败恢复增量通过 Standards/Spec 双轴审查，无P0/P1/P2/P3；未改变市场筛选、PAPER交易、钱包/LIVE或最终UI边界。
- 实际构建产物用独立SQLite和停止策略运行2.2秒资源留证冒烟：5个样本、传输错误0、关键错误0，样本和汇总均包含运行时长及四项内存指标；扫描尚未完成，因此5个样本均有预期告警。证据为`data/validation/runtime-metrics-smoke-20260805.jsonl`（SHA-256 `b6c7cc15...297f`），该短时结果只验证接线，不是服务器容量基线。
- 资源留证增量首审的1项P2已通过聚合峰值类型和统一更新函数修复，对外JSONL字段保持扁平不变；Standards/Spec复审均无P0/P1/P2/P3。

每次交接前必须重新运行验证，并在本节记录结果。

## 接手步骤

```bash
git clone https://github.com/yvettemiranda/PM-SMALL.git
cd PM-SMALL
cp .env.example .env
npm ci
npm run typecheck
npm test
npm run build
```

确认工作区干净且验证通过后，从“下一步”继续。不得根据旧聊天记录推断进度，仓库文件和GitHub `main`提交记录为唯一依据。

## 安全提醒

- 不得在聊天、代码、提交记录或Issue中粘贴钱包私钥、助记词和访问令牌；
- 实盘功能启用前必须先完成纸面闭环及长时间验证；
- 若密钥曾进入Git历史，仅删除文件不够，必须立即停用并更换密钥。
