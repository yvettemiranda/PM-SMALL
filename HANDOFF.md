# 项目交接记录

更新时间：2026-08-03（UTC）

## 当前结论

项目处于`v0.1.0`基础阶段，只能进行手动触发的纸面订单测试。纸面交易闭环尚未完成，不能接入钱包或用于实盘。

本文件所在的`main`分支最新提交为当前交接基线，具体版本以Git提交记录为准。

## 已完成

- 建立Node.js 24、TypeScript、Fastify、SQLite项目结构；
- 接入Polymarket公开市场数据；
- 实现事件过滤、候选Token提取和市场扫描；
- 实现1至3美分Maker买价、单笔1U和固定卖价计算；
- 实现保守纸面队列成交模型；
- 建立纸面订单、成交、持仓、策略状态和审计表；
- 提供基础网页界面和状态、候选市场、纸面买单接口；
- 实盘执行器固定禁用；
- 建立Docker、自动测试和跨账号交接基础。

## 尚未完成

- 公开市场WebSocket连接、订阅、断线重连和数据完整性控制；
- 实时成交驱动的纸面部分成交与去重；
- 每批买入成交后自动建立固定价格卖单；
- 首次卖出成交后撤销该Token剩余买单，并永久停止该轮新买；
- 比赛开始停止买单、事件结算、纸面赎回和完整状态恢复；
- 长时间纸面运行验证；
- 钱包、签名、实盘订单、撤单、对账和链上赎回。

## 下一步

开发`v0.2.0`纸面实时成交阶段：

1. 增加公开市场WebSocket适配器；
2. 处理成交序列、去重、断线重连和数据不完整状态；
3. 自动应用保守队列纸面成交；
4. 买入成交后自动创建对应纸面卖单；
5. 增加部分成交、断线重连、自动卖单和首次卖出规则测试。

本阶段仍不得接入钱包、提交真实订单或执行链上写操作。

## 最近验证

基线建立前已通过：

- `npm run typecheck`；
- `npm test`：8个测试文件、18项测试通过；
- `npm run build`；
- `npm audit --omit=dev`：0项漏洞。

每次交接前必须重新运行验证，并在本节记录结果。

## 接手步骤

```bash
git clone https://github.com/yvettemiranda/PM-SMALL.git
cd PM-SMALL
cp .env.example .env
npm ci
npm run typecheck
npm test
npm run build
```

确认工作区干净且验证通过后，从“下一步”继续。不得根据旧聊天记录推断进度，仓库文件和Git提交记录为唯一依据。

## 安全提醒

- 不得在聊天、代码、提交记录或Issue中粘贴钱包私钥、助记词和访问令牌；
- 实盘功能启用前必须先完成纸面闭环及长时间验证；
- 若密钥曾进入Git历史，仅删除文件不够，必须立即停用并更换密钥。
