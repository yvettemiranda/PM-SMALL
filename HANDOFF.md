# 项目交接记录

更新时间：2026-08-07

## 当前结论

PM-SMALL 已完成最终 TEST 需求的本地实现、自动验证、桌面/手机浏览器检查、双轴代码审查、GitHub 同步和 Linux 服务器短时部署冒烟。服务器运行时代码对应 GitHub 功能提交 `40a09e36a9cb1bd27f3b39a11bc96b71083ba4ee`；当前只允许 `TEST + LIVE_DISABLED`：不接钱包、不签名、不发送真实订单、不执行链上写操作。

本轮正式把生命周期进度恢复为硬筛选条件，默认 `<=20%`；同时统一了最低 1¢、最高 1–3¢、有效 Best Bid、Bid/Ask 比例、订单簿完整性、最小下单量、市场时长、官方类别和体育开赛等全部资格规则。部署时发现并修复了旧多档 FAK 买单的成交份额不变量，Schema 13 已无损归一旧记录。短时冒烟已经完成，当前已按用户要求停在正式长期验证前。

GitHub `main` 是唯一交付进度依据。任何本地未推送内容或服务器临时状态都不算正式进度。

## 当前业务逻辑

1. 程序启动后立即扫描 Polymarket 全部开放事件分页，不使用事件日期窗口或隐藏数量上限。每轮完成后等待 15 秒再开始下一轮，多轮不重叠；失败时保留上一轮市场和 WebSocket 监控。
2. 结构市场必须满足：事件开放且未关闭/归档；具体市场 active、未关闭/归档、允许下单、启用订单簿且有有效 Condition ID；已经开始且尚未结束。
3. 总生命周期按具体市场自身时间计算，缺失才回退事件时间；固定 `>=1天`，并 `<=` 用户选择的节点 `1、7、14、30、60、90、120、180、360、365`，默认 30 天。
4. 当前资格还必须同时满足：勾选的二元/三元、官方类别、完整订单簿、Best Ask 在 `1¢` 至用户上限、有效 Best Bid、`Best Bid / Best Ask` 达到配置比例（默认 50%）、生命周期进度不超过配置（默认 20%）、每轮金额满足平台最小下单量、tick 有效、体育比赛尚未开始。
5. 所有结构合格 Token 都保留在监控集合中；页面“扫描市场”和新买入只使用当前满足全部资格的子集。Bid、Ask、比例和时间进度变化后会按 WebSocket/当前时间即时重新判断，不等下一轮全扫。
6. 类别只使用并保存 Polymarket 官方 Tag ID，一个市场可以属于多个 Tag。没有官方标签的市场在“全部类别”模式下仍可参与，但不会生成伪造的 `OTHER` 类别。
7. TEST 买入按实时订单簿执行限价 FAK：从最低 Ask 逐档成交，只吃用户上限内的深度；允许部分成交，未成交余量立即取消，只扣实际金额。
8. 每个 Token 每轮累计投入上限由用户设置，默认 1U。新 Token 或新周期只有可用现金至少等于完整配置金额时才开启；已有部分周期可以在新盘口出现后用可用现金补到剩余额度。
9. 每笔买入填充独立创建目标：`max(买价 + 1¢, 买价 × 1.5)`，再按 tick 向上取整。创建后立即复查同一盘口；Best Bid 达标即按 FAK 从高到低卖出，允许部分成交，余量继续等待原目标。
10. 已消费的 Ask/Bid 深度按单侧完整盘口状态的稳定版本持久化到 SQLite。重复增量、等价完整快照、断线重连和进程重启不得重复使用同一深度；实际盘口状态变化后才形成新版本。
11. 首次发生卖出后，该 Token 本轮停止继续买入。全部卖完后周期关闭；若市场仍满足资格且未正式结算，资金条件满足时自动进入下一轮，不提供手动新一轮入口。
12. 正式关闭且官方状态为 `resolved` 或 `settled` 后自动执行 TEST 结算和模拟赎回；Condition ID 幂等，只接受明确的二元 1/0 或官方 0.5/0.5 结果。
13. 动态费用计入买入份额、卖出到账和盈亏。持仓用当前可执行 Best Bid 扣预计费用估值；完整盘口没有 Bid 时可兑现价值为 0，但持仓仍继续显示和管理。
14. 暂停只阻止新买；已有目标卖出、减仓与正式结算继续运行。账本验证失败会保留现场并暂停增仓。

## 配置与 UI

- 手机优先、桌面与手机共用紧凑单列页面，至少支持 320px；默认仅展示配置入口、资金盈亏、当前持仓和扫描市场。
- 左上角只有 `TEST / LIVE`，LIVE 始终锁定。
- 配置包含：官方类别多选、二元/三元、买价 1–3¢、最低 Bid/Ask 比例 1–100%（默认 50%）、最大生命周期进度 1–100%（默认 20%）、总时长节点、TEST 总资金和每 Token 每轮金额。
- TEST 初始资金默认 100U，只能在 `PAUSED` 且没有任何交易历史时修改；已有历史必须先完成双重确认的 TEST 重置。
- 持仓显示全部正数量 Token，不限前 10 个；突出结果方向、实际加权买入价、当前可卖价、目标档位、数量和市场全生命周期进度。
- 当前可卖价明确区分实际 Best Bid、暂无买盘、行情未就绪、重连中和已断开，不统一显示 0.00¢。
- 扫描市场默认前 20 项并可展开，支持生命周期进度正序/倒序；价格以两位小数美分显示，页面约每 500ms 自动更新且不覆盖未保存配置草稿。

## 数据库与升级

- 当前 Schema 为 13。Schema 12 新增最低 Bid/Ask 比例、官方类别 ID/标签和 `test_order_book_consumption`；Schema 13 无损归一旧版已完全成交 FAK 买单的 `original_size_micros`，不改资金、成交、持仓或盈亏。
- 每个多语句迁移及其版本登记在同一个 SQLite 事务中执行；失败会整体回滚，避免半迁移数据库。
- Schema 12 会把升级库强制切到 `PAUSED`，不会沿用旧 `RUNNING` 状态自动开始新规则交易。
- Schema 12 将旧兼容值 `max_market_progress_percent=100`迁移到新默认 20，其他有效用户设置保持不变。
- Schema 13 同样强制 `PAUSED`，并保证现金完全用完的 FAK 买单满足 `FILLED => original_size_micros = filled_size_micros`。
- TEST 重置会清空订单、成交、持仓、结算、配置、审计和盘口消费，并恢复 100U、1U、50%、20%、30 天和 `PAUSED`。

## 已完成验证

- 全量自动测试：26 个文件、182 项全部通过。
- TypeScript 类型检查、生产构建和 `git diff --check` 通过。
- 覆盖统一资格、15 秒非重叠扫描、扫描失败保留、实时资格变化、FAK 买卖、部分成交、多档目标、费用、资金上限、50 个满额 Token、101 个持仓返回、自动下一轮、正式结算、重置和 LIVE 禁用。
- 盘口消费覆盖重复 `price_change`、等价快照、增量更新后重连、SQLite 重启恢复和无关侧变化，均不重复成交。
- Schema 12 覆盖统一资格升级与原子回滚；Schema 13 覆盖旧 FAK 订单无损归一和新订单份额不变量。
- 双轴代码审查最终复审均为无 findings。
- 本地 UI 已在桌面与 390px 手机浏览器检查：单列无横向溢出，比例/进度默认值、资金编辑状态和 LIVE 锁正常，控制台无错误。

## 服务器交付结果

- 部署前原始备份：`/home/ubuntu/pm-small-backup-20260807T015306Z`；Schema 12 现场备份：`/home/ubuntu/pm-small-backup-20260807T020959Z`。两份备份均通过 SHA-256 和隔离 SQLite 完整性检查，未删除旧备份。
- 容器重建和重启后均为 healthy；应用仅监听 `127.0.0.1:3000`，Nginx 监听公网 80/443，配置检查通过。
- 公网严格检查通过：HTTP 跳转、未登录 401、登录后页面 200、`TEST + LIVE_DISABLED + PAUSED`、TEST 验证和 SQLite 均正常。访问地址为 `https://43-159-133-129.sslip.io/`。
- 服务器现有 1000U 历史 TEST 账本保留；当前最大时长已设为 30 天。`.env` 的重置默认值已校正为 100U 总资金、100U 总预算、1U 每轮、30 天。

## 尚未完成

- 由用户制定的正式长期 TEST 验证计划及执行。
- 长期接口限流、全量扫描耗时、WebSocket 容量、断线恢复、服务器资源和真实公开行情成交样本的最终验收。
- 钱包、签名、LIVE 下单/撤单/对账和链上赎回。

## 下一步与停止条件

1. 当前停止继续运行验证，不自行启动 TEST，不把短时冒烟计入正式长期验证。
2. 由用户制定正式长期 TEST 验证计划，包括时长、故障场景、资源门槛、真实成交样本和通过标准。
3. 计划确认后再执行长期验证；任何失败都保留现场并维持 LIVE 禁用。

## 接手步骤

```bash
git clone https://github.com/yvettemiranda/PM-SMALL.git
cd PM-SMALL
cp .env.example .env
npm ci
npm run typecheck
npm test
npm run build
```

继续前依次阅读本文件、`docs/DECISIONS.md`、`docs/PLAN.md`、`SECURITY.md` 和 `docs/SERVER_DEPLOY.md`。任何历史方案与现行决定冲突时，以 GitHub `main` 上的 `docs/DECISIONS.md` 为准。
