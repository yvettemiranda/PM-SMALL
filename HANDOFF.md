# 项目交接记录

更新时间：2026-08-06（UTC）

## 当前结论

PM-SMALL 已完成 v0.4.0 的 TEST 结算闭环，并完成 v0.5.0 的交易模型与最终 UI 本地实现。当前仍只允许 `TEST + LIVE_DISABLED`：不接钱包、不签名、不发送真实订单、不执行链上写操作。

本轮把旧的等待成交 Maker 模型改为可执行盘口驱动的 FAK 模型，并按用户最终确认修正市场时长定义：每个具体市场的总时长必须满足 `1 天 <= 结束时间 - 开始时间 <= 滑杆上限`。默认上限 30 天，即筛选 1 至 30 天的市场；剩余时间不参与时长筛选，生命周期进度只用于展示和正序/倒序。

本文件所在的 GitHub `main` 最新提交是唯一交付进度依据。本轮代码已经完成本地实现、浏览器验收和双轴审查修正；GitHub 推送与服务器更新状态以本文件后续“最近交付”记录为准。

## 当前业务逻辑

1. 遍历 Polymarket 全部开放事件分页，不用事件日期窗口或本地数量上限截断；再按每个具体市场自身时间做精确筛选。
2. 市场必须满足：事件开放且未关闭/归档；具体市场 active、未关闭/归档、允许下单、启用订单簿且有 Condition ID；总时长 1 天至所选上限；结果数量为已选择的二元和/或三元；类别符合配置。体育市场在比赛开始后停止新买。
3. 不设置历史成交量硬门槛。所有结构合格 Token 都进入盘口监控；只有当前 Best Ask 大于 0、低于等于买价上限，且单轮金额满足订单簿最小下单量时，才成为当前可买候选。
4. TEST 买入按实时订单簿 Best Ask 执行 FAK：逐档成交，可部分成交，未成交余量立即取消，不产生长期等待的买单，也不重复使用已消耗深度。每个 Token 每轮累计使用金额默认 1U。
5. 每笔买入成交立即创建目标卖出价：`max(买价 + 1¢, 买价 × 1.5)`，再按 tick 向上取整。Best Bid 达到目标后立即按 FAK 卖出；若买入完成时同一盘口已经满足目标，会在同一次处理内再次检查并卖出。
6. 首次卖出后本轮停止继续买入；部分卖出继续等待剩余目标。完整卖出且市场仍开放、未正式结算时，下一次合格机会自动开始下一轮，不提供手动“新一轮”入口。
7. 市场正式关闭且官方结果为 `resolved` 或 `settled` 后自动做 TEST 结算和模拟赎回；Condition ID 保证幂等。支持二元 1/0 和官方 0.5/0.5 结果。
8. 买卖使用市场动态费率；买入费减少所得份额，卖出费减少到账金额。持仓以当前可执行 Best Bid 扣除预计卖出费用估值；明确没有买盘时可兑现价值按 0 计算。
9. 暂停只阻止新买并撤销仍活动的旧买单；已有持仓、目标卖出和结算继续管理。账本验证失败同样冻结增仓，但允许减仓。
10. TEST 默认总资金 100U、每个 Token 每轮 1U、初始状态 `PAUSED`。重置必须先暂停并完成两次确认，随后清空 TEST 订单、成交、持仓、结算、配置和审计并恢复默认值。

## UI

- 手机优先、桌面与手机共用单列布局，至少 320px 无横向溢出。
- 左上角只有 `TEST / LIVE`；LIVE 入口锁定。
- 默认只展示交易配置入口、总资金/总盈亏/已实现/未实现、当前持仓和扫描市场；不展示最近活动或程序诊断卡片。
- 配置包含：类别多选、二元/三元多选、最高买价 1–3¢、总时长节点 `1、7、14、30、60、90、120、180、360、365`、总模拟资金和每轮金额。
- 不提供逐 Token 勾选。符合配置的市场都自动参与 TEST。
- 持仓突出结果方向、平均买入价、当前 Best Bid、目标卖价、数量和市场全生命周期进度。
- 扫描市场显示当前买卖价、开始/结束时间和进度；支持进度正序/倒序，默认显示前 20 项并可展开。
- 后端由公开 WebSocket 行情触发，页面约每 500ms 刷新；价格按两位小数美分显示。

## 已完成验证

- TEST FAK 买入、部分成交、目标卖出、动态费用、首次卖出锁、自动下一轮和正式结算均有自动回归。
- 市场总时长下限 1 天、滑杆上限、具体市场时间优先、99% 生命周期仍可参与，以及不使用事件日期窗口截断均有固定回归。
- 买入后同一盘口立即复查目标卖出；旧 v0.4 卖单迁移后具备可退出的兼容最小下单量。
- 配置空结果拒绝、类别筛选、无买盘估值为 0、TEST 重置和 LIVE 禁用均有回归。
- 本地最终 UI 已在桌面、390px 和 320px 浏览器验收：无横向溢出、配置草稿不被轮询覆盖、开始/暂停及 LIVE 锁正常、控制台无错误。
- 双轴代码审查发现的具体市场时长、同盘口买后卖、旧卖单迁移、逐 Token 残留和手动新一轮入口问题均已修正。

## 尚未完成

- 本轮最新提交的 GitHub 远端同步和 Linux 服务器同提交部署（若“最近交付”尚未记录完成）。
- 由用户制定的正式长期 TEST 验证计划及执行。
- 长期接口限流、全量扫描耗时、WebSocket 容量、断线恢复、服务器资源和真实公开行情成交样本的最终验收。
- 钱包、签名、LIVE 下单/撤单/对账和链上赎回。

## 下一步与停止条件

1. 完成本地全量测试、类型检查、构建和文档检查。
2. 推送 GitHub `main` 并核对远端 SHA。
3. 停止服务器应用，备份 SQLite 与部署配置并校验；部署同一 SHA，保持 `TEST + LIVE_DISABLED + PAUSED`，完成 HTTPS、认证、API、数据库迁移和重启短时冒烟。
4. 到此停止。不得自行开始正式长期验证；由用户先制定时长、故障场景和验收标准。

## 最近交付

- 上一服务器版本通过证书匹配的 HTTPS 主机名提供手机/电脑访问，Nginx Basic Authentication 生效，应用 3000 端口只绑定 `127.0.0.1`。
- 本轮最新本地功能基线为 `fe5ff57` 及其后的审查修正；尚未在本段记录 GitHub/服务器完成前，不得把它视为已经远端交付。

## 接手步骤

```bash
git clone https://github.com/yvettemiranda/PM-SMALL.git
cd PM-SMALL
cp .env.example .env
npm ci
npm run typecheck
npm test
npm run build
```

继续前依次阅读本文件、`docs/DECISIONS.md`、`docs/PLAN.md`、`SECURITY.md` 和 `docs/SERVER_DEPLOY.md`。任何历史方案与现行决定冲突时，以 GitHub `main` 上的 `docs/DECISIONS.md` 为准。
