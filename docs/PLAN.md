# 开发计划

现行业务规则见 [`DECISIONS.md`](DECISIONS.md)。历史完整方案仅作背景；发生冲突时以 `DECISIONS.md` 和 GitHub `main` 代码为准。

## 阶段状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| v0.1.0 | 项目结构、公开数据、基础筛选、SQLite 和基础界面 | 已完成 |
| v0.2.0 | 公开 WebSocket、成交去重、自动卖出和首次卖出锁 | 已完成 |
| v0.3.0 | 自动交易、体育开始控制、重启恢复 | 已完成 |
| v0.4.0 | 正式结果判断、TEST 结算和模拟赎回闭环 | 已完成 |
| v0.5.0 | 全市场精确筛选、统一硬资格、FAK 买卖、自动循环、最终 UI、Linux 部署与长期验证 | 功能、审查、GitHub 与 Linux 短时交付已完成；正式长期验证计划待用户确认 |
| v0.8.0 | 钱包、LIVE 执行适配器、对账和安全门 | 禁止开始，等待 v0.5.0 正式验收 |
| v0.9.0 | 实盘部署加固与经重新授权的小额验收 | 待开发 |
| v1.0.0 | 运维、备份、恢复、监控和最终交付 | 待开发 |

## v0.5.0 完成标准

- 遍历全部开放事件分页，并通过同一资格函数按具体市场的用户自定 1–365 天总时长区间、开放/可下单/完整订单簿、结果数量、Polymarket 首页栏目、1–3¢、有效 Best Bid、Bid/Ask 比例、生命周期进度、最小下单量和体育开赛时间精确筛选；不使用事件日期窗口或本地数量上限漏掉候选。
- TEST 以实时 Best Ask/Best Bid 执行 FAK 买卖，支持部分成交、稳定盘口版本与跨重启消费、动态费用、每笔填充目标、首次卖出锁、资金释放重试和完整卖出后自动下一轮。
- 正式关闭后自动 TEST 结算和模拟赎回，账本幂等、守恒、可审计、可恢复。
- 手机和电脑使用同一紧凑单列 UI；配置、资金、持仓和扫描市场行为与后端一致。
- 全量自动测试、类型检查、构建、桌面/手机浏览器验收和双轴代码审查通过；数据库升级原子执行并失败关闭到 `PAUSED`。
- GitHub `main` 与 Linux 服务器运行同一提交；部署前有可核验的 SQLite 和配置备份，部署后保持 `TEST + LIVE_DISABLED + PAUSED`并通过短时冒烟。
- 用户制定并批准正式长期验证计划后，完成约定时长、故障场景、资源/接口容量和数据一致性验收。

## 当前执行顺序

1. 已在 Schema 14 UI 基础上完成流式事件处理、静态预筛、有界并发盘口、增量行情订阅、候选重连保留、扫描诊断和大集合热路径修复；真实多轮扫描、优雅停止及 462px/320px UI 检查均已通过。
2. 长期 TEST 前正确性加固已完成本地 red→green：断线 stale-ready、行情/费用/开放状态边界、首页栏目白名单、盘口身份、结算向量与重放身份、fill 账本证明和 soak 响应解析均有回归；全量 26 文件、227 项通过。
3. 固定点 Standards / Spec 最终复审均为 0 findings；运行时代码 `ff8b5bc9010261a349f27d7640c17789896f38d0` 已推送 GitHub 并部署 Linux。部署前备份与隔离 SQLite 校验、容器重启、HTTPS/认证、端口、账本和安全状态冒烟均已通过，最终保持 `TEST + LIVE_DISABLED + PAUSED`。
4. 当前已停止在正式长期 TEST 前；先复核本轮发现的异常启动来源与启动访问边界，再由用户确认长期验证时长、故障场景、资源门槛、成交样本、证据格式、通过标准和停止条件。
5. 计划确认后执行长期验证并保存证据；失败时保留现场，不绕过安全门。只有正式验收完成且用户重新明确授权后，才可讨论钱包和 LIVE 实现。

## 持续边界

- 任何阶段都不得用空数据、旧盘口或推测结果冒充成交或结算。
- LIVE 在 v0.5.0 正式长期验证完成前始终禁用；实现和测试通过不等于实盘授权。
- 新增交易状态必须具备幂等、重启恢复、资金守恒和审计记录。
- 每次交付同步更新 `HANDOFF.md`、本文件及必要的变更记录，并核对 GitHub 远端 SHA。
