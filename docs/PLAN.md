# 开发计划

现行业务规则见 [`DECISIONS.md`](DECISIONS.md)。历史完整方案仅作背景；发生冲突时以 `DECISIONS.md` 和 GitHub `main` 代码为准。

## 阶段状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| v0.1.0 | 项目结构、公开数据、基础筛选、SQLite 和基础界面 | 已完成 |
| v0.2.0 | 公开 WebSocket、成交去重、自动卖出和首次卖出锁 | 已完成 |
| v0.3.0 | 自动交易、体育开始控制、重启恢复 | 已完成 |
| v0.4.0 | 正式结果判断、TEST 结算和模拟赎回闭环 | 已完成 |
| v0.5.0 | 全市场精确筛选、任意标准多元 Event 仲裁、Event 周期锁、FAK 买卖、自动循环、最终 UI、Linux 部署与长期验证 | Schema 15 功能、自动验证、本地 PAUSED UI 验收、GitHub/Linux 部署与公网冒烟已完成；累计 72 小时正式验证方案已确认，待用户另行指令开始执行 |
| v0.8.0 | 钱包、LIVE 执行适配器、对账和安全门 | 禁止开始，等待 v0.5.0 正式验收 |
| v0.9.0 | 实盘部署加固与经重新授权的小额验收 | 待开发 |
| v1.0.0 | 运维、备份、恢复、监控和最终交付 | 待开发 |

## v0.5.0 完成标准

- 遍历全部开放事件分页，并通过同一资格函数按具体市场的用户自定 1–365 天总时长区间、开放/可下单/完整订单簿、`BINARY / TERNARY / MULTI` 类型、Polymarket 首页栏目、`0.1¢–99¢` 自定买价闭区间、有效 Best Bid、Bid/Ask 比例、生命周期进度、最小下单量和体育开赛时间精确筛选；标准 Event 统一支持安全整数 `resultCount >= 2`，不使用事件日期窗口或本地数量上限漏掉候选。
- 未锁 Event 只有全部静态合格兄弟 Token 的盘口新鲜完整时才执行 FAK Preview 和确定性仲裁；执行前必须重算。第一次实际 Fill 在同一事务创建 Event 锁、成交、仓位、targets、资金和盘口消费；0 Fill 不锁。
- TEST 以实时 Best Ask/Best Bid 执行 FAK 买卖，支持部分成交、稳定盘口版本与跨重启消费、动态费用、每笔 Fill 按用户配置的 `min(99¢, tick↑ max(买价+A, 买价×B))` 创建冻结目标、Event 级冻结预算、首次卖出后整 Event 禁买、资金释放重试和完整卖出后自动重新仲裁下一轮。
- 正式关闭后自动 TEST 结算和模拟赎回，账本幂等、守恒、可审计、可恢复。
- 手机和电脑使用同一紧凑单列 UI；配置、资金、持仓、默认折叠的交易记录和扫描市场行为与后端一致。
- 全量自动测试、类型检查、构建、桌面/手机浏览器验收和双轴代码审查通过；数据库升级原子执行并失败关闭到 `PAUSED`。
- GitHub `main` 与 Linux 服务器运行同一提交；部署前有可核验的 SQLite 和配置备份，部署后保持 `TEST + LIVE_DISABLED + PAUSED`并通过短时冒烟。
- 用户制定并批准正式长期验证计划后，完成约定时长、故障场景、资源/接口容量和数据一致性验收。

## 当前执行顺序

1. Schema 15 任意标准多元升级已完成：安全整数结果数、三类配置、Token/Event 索引、无副作用 FAK Preview、完整兄弟盘口门槛、Event 仲裁、原子锁、冻结预算、退出/结算解锁、迁移兼容、验证、API/UI 与诊断均已落地。
2. 本地全量 32 个测试文件、292 项测试、TypeScript 类型检查、生产构建、前端语法、正式 TEST 包装脚本语法与差异检查均通过；462px/320px PAUSED UI 无横向溢出、无控制台错误，未启动服务器正式 TEST。
3. Standards / Spec 双轴复审为 0 findings；运行时代码 `cb1472c90039ed72e9038821434cf22b45153f43` 已推送 GitHub 并部署。备份哈希、隔离 SQLite、Schema 15 迁移、同库重启、账本、端口、HTTPS/认证和公网 PAUSED 冒烟均通过。
4. 此前 100U→1000U 与 RUNNING 已由用户确认是手动操作并结案。正式 TEST 采用累计 72 小时、每 4 小时与配置变化切段、逐段由用户确认计入的方案；独立监控只读状态，不启动第二套全市场扫描。
5. 当前只部署管理工具并保持 PAUSED；用户另行明确开始后才执行。只有正式验收完成且用户重新明确授权后，才可讨论钱包和 LIVE 实现。

## 持续边界

- 任何阶段都不得用空数据、旧盘口或推测结果冒充成交或结算。
- LIVE 在 v0.5.0 正式长期验证完成前始终禁用；实现和测试通过不等于实盘授权。
- 新增交易状态必须具备幂等、重启恢复、资金守恒和审计记录。
- 每次交付同步更新 `HANDOFF.md`、本文件及必要的变更记录，并核对 GitHub 远端 SHA。
