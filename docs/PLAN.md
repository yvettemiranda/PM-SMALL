# 开发计划

现行业务规则见 [`DECISIONS.md`](DECISIONS.md)。历史完整方案仅作背景；发生冲突时以 `DECISIONS.md` 和 GitHub `main` 代码为准。

## 阶段状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| v0.1.0 | 项目结构、公开数据、基础筛选、SQLite 和基础界面 | 已完成 |
| v0.2.0 | 公开 WebSocket、成交去重、自动卖出和首次卖出锁 | 已完成 |
| v0.3.0 | 自动交易、体育开始控制、重启恢复 | 已完成 |
| v0.4.0 | 正式结果判断、TEST 结算和模拟赎回闭环 | 已完成 |
| v0.5.0 | 全市场精确筛选、FAK 买卖、自动循环、最终 UI、Linux 部署与长期验证 | 开发中：代码和本地短时验证完成；待 GitHub/服务器同步及用户制定正式长期验证计划 |
| v0.8.0 | 钱包、LIVE 执行适配器、对账和安全门 | 禁止开始，等待 v0.5.0 正式验收 |
| v0.9.0 | 实盘部署加固与经重新授权的小额验收 | 待开发 |
| v1.0.0 | 运维、备份、恢复、监控和最终交付 | 待开发 |

## v0.5.0 完成标准

- 遍历全部开放事件分页，并按每个具体市场的总时长 `1 天至所选上限`、开放/可下单/有订单簿、结果数量和类别精确筛选；不使用事件日期窗口或本地数量上限漏掉候选。
- TEST 以实时 Best Ask/Best Bid 执行 FAK 买卖，支持部分成交、盘口深度消费、动态费用、目标价、首次卖出锁和完整卖出后自动下一轮。
- 正式关闭后自动 TEST 结算和模拟赎回，账本幂等、守恒、可审计、可恢复。
- 手机和电脑使用同一紧凑单列 UI；配置、资金、持仓和扫描市场行为与后端一致。
- 全量自动测试、类型检查、构建、桌面/390px/320px 浏览器验收和双轴代码审查通过。
- GitHub `main` 与 Linux 服务器运行同一提交；部署前有可核验的 SQLite 和配置备份，部署后保持 `TEST + LIVE_DISABLED + PAUSED`并通过短时冒烟。
- 用户制定并批准正式长期验证计划后，完成约定时长、故障场景、资源/接口容量和数据一致性验收。

## 当前执行顺序

1. 修完审查问题并完成本地全量验证。
2. 更新交接、决策、部署和长期验证文档。
3. 提交并推送 GitHub `main`，核对远端 SHA。
4. 停止服务器、备份 SQLite 与配置、部署同一 SHA，执行短时冒烟和同库重启检查。
5. 在正式长期验证前停止，由用户制定验证计划。

## 持续边界

- 任何阶段都不得用空数据、旧盘口或推测结果冒充成交或结算。
- LIVE 在 v0.5.0 正式长期验证完成前始终禁用；实现和测试通过不等于实盘授权。
- 新增交易状态必须具备幂等、重启恢复、资金守恒和审计记录。
- 每次交付同步更新 `HANDOFF.md`、本文件及必要的变更记录，并核对 GitHub 远端 SHA。
