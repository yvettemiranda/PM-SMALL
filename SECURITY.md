# 安全说明

## 当前状态

当前版本只允许 `TEST` 模式；内部 `paper_*` 命名仅为兼容实现。实盘执行器固定禁用，不读取钱包或交易凭据。

## 禁止提交

以下内容不得进入代码、Git历史、Issue、Pull Request、Actions日志或聊天：

- 私钥、助记词、Seed Phrase；
- API Key、Secret、访问令牌和Cookie；
- `.env`及其他真实环境配置；
- PAPER或LIVE数据库、备份和运行日志；
- 服务器密码、SSH私钥和恢复码。

仓库已经公开。即使随后删除文件，历史提交仍可能保留内容。如发生泄露，必须立即停用并更换相关密钥。

## 钱包要求

- 只使用专门为本项目建立的钱包；
- 初次实盘只存放可承受损失的小额资金；
- 密钥只在用户控制的Mac或服务器上配置；
- 实盘默认关闭，程序重启后不得自动恢复下单；
- 实盘前必须通过余额、授权、网络、时间同步、订单对账和紧急停机检查。

## 数据要求

- `data/`、数据库和日志保持在仓库外；
- 服务器运行数据应定期加密备份；
- PAPER和LIVE使用不同数据库；
- 不确定订单和成交状态必须停止新订单，完成对账后才能恢复。

## 报告问题

发现可能导致资金损失或密钥泄露的问题时，应立即停止程序并撤销实盘权限。公开仓库中不要提交包含敏感细节的Issue。
